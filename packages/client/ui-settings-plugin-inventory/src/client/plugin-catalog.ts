/** Plugin catalog: human summary and disable impact per module. Generated — do not edit manually. */
export interface PluginMeta { readonly summary: string; readonly impact: string }
const CATALOG: Record<string, PluginMeta> = {
  '@deepseek-ai/cordis': { summary: 'Meta-Framework for Modern JavaScript Applications', impact: 'Cordis core framework. Disabling stops the entire harness.' },
  '@deepseek-ai/cordis-plugin-group': { summary: 'Nested plugin group for cordis', impact: 'Group container for isolated plugin scopes. Disabling flattens isolation.' },
  '@deepseek-ai/cordis-plugin-hmr': { summary: 'Hot Module Replacement Plugin for Cordis', impact: 'Hot-reload for client plugins during dev. Disabling forces a full host restart to pick up client changes.' },
  '@deepseek-ai/cordis-plugin-include': { summary: 'Include files in cordis configurations', impact: 'Loads cordis.yml / patch layers. Disabling prevents composition of the plugin tree.' },
  '@deepseek-ai/cordis-plugin-loader': { summary: 'Plugin loader for cordis', impact: 'The plugin loader itself. Disabling stops all plugin lifecycle; host becomes inert.' },
  '@deepseek-ai/cordis-plugin-logger-console': { summary: 'Console logger exporter for cordis', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/cordis-plugin-timer': { summary: 'Timer service for cordis', impact: 'Timer service for delayed tasks. Disabling breaks any plugin using timers.' },
  '@deepseek-ai/cosmokit': { summary: 'A collection of common utilities', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-acp': { summary: 'Automation-only Agent Client Protocol server for driving DeepSeek Harness agents over JSON-RPC stdio', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-acp-demo': { summary: 'ACP automation server app: agent spine + JSONL persistence + ACP transport, with a JSON-RPC stdio bin', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-acp-snapshot': { summary: 'ACP test kit: shared subprocess launcher, snapshot scenario harness, expected-output normalizers, and suite factory', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-agent': { summary: 'Agent interface, registry, initiator scope, and event vocabulary for the DeepSeek Harness', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-agent-default-model': { summary: 'Default model selection shared by Agent entry points', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-agent-instructions': { summary: 'Workspace context loader for AGENTS.md/CLAUDE.md instruction files', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-agent-loop': { summary: 'The concrete agent loop plugin for the DeepSeek Harness', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-agent-loop-testkit': { summary: 'Shared prerequisite mounting for tests that exercise the concrete agent loop', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-agent-presets': { summary: 'Per-session agent composition from preset cordis.yml files for the DeepSeek Harness', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-agent-spine-demo': { summary: 'The default executor-less/UI-less agent spine with fallback session titles, provider-routed retry, and optional persisted goals', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-agent-tool-presentation': { summary: "Agent-plane presentation selector: composes one agent's tools as Code Mode, native, or both", impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-anonymous-user-id': { summary: 'Shared anonymous user identity for DeepSeek Harness telemetry and feedback correlation', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-api-gateway': { summary: 'Typert Remote Host dispatcher and Client API endpoint', impact: 'RPC gateway. Disabling breaks all Remote calls (settings, inventory, etc.).' },
  '@deepseek-ai/dsh-api-remotes': { summary: 'Remote BFF assembly and Host Agent/Session lookup policy', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-app-boot': { summary: 'Shared boot glue for the app bins: .env loading, fail-loud Loader guards, snapshot-aware config resolution, and the Loader boot sequence', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-atomic-write': { summary: 'Zero-dependency atomic file replacement: exclusive-create random-suffix temp + rename carrying the caller-stated permissions (writeFileAtomic)', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-attachment': { summary: 'Durable immutable attachment storage seam for the DeepSeek Harness', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-attachment-local': { summary: 'Private content-addressed DSH_HOME attachment storage', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-base': { summary: "The shared dsh core as a profile bundle: every profile's first patch layer, inserting the base plugin rows over the empty profile root", impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-bash-local': { summary: 'Local-subprocess implementation of the DeepSeek Harness bash executor seam', impact: 'Shell command execution. Disabling removes the agent’s ability to run commands.' },
  '@deepseek-ai/dsh-bash-sandbox': { summary: 'Sandbox-consuming implementation of the DeepSeek Harness bash executor seam (confines every command via ctx.sandbox, reports denial/enforcement result facts)', impact: 'Shell command execution. Disabling removes the agent’s ability to run commands.' },
  '@deepseek-ai/dsh-brand': { summary: 'Type-only Branded<B> nominal-typing primitive for the DeepSeek Harness', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-client-connection': { summary: 'Wire consumer layer: HTTP-up/WebSocket-down client, ConnectionController dual streams with reconnect, and fixture api', impact: 'WebSocket/Fetch bridge to host. Disabling disconnects the browser (offline).' },
  '@deepseek-ai/dsh-client-hmr': { summary: 'Dev-only hot-reload driver for script-loaded client entries: SSE rebuilt frames → invalidate/prefetch → fiber swap through the vendored Loader entry', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-client-locale': { summary: 'Locale plugin: Host-backed zh/en preference, browser-derived fallback, locale snapshots, and typed namespace dictionaries', impact: 'Appearance/localization. Disabling affects UI theme/language only.' },
  '@deepseek-ai/dsh-client-modules': { summary: 'Client module system, dual-face: node half composes the __DSH_BOOT__ entry graph (incremental dsh.client scan, bundle route, index tap, webPlugins service); browser half is the lazy-CJS module table t', impact: 'Client module table. Disabling disconnects all browser plugins from the host.' },
  '@deepseek-ai/dsh-client-runtime': { summary: 'Client core services: SlotRegistry, SessionRuntime (scope tree + object layer)', impact: 'Client runtime and slot registry. Disabling breaks all UI slots.' },
  '@deepseek-ai/dsh-client-test-runtime': { summary: 'jsdom slot test runtime: real Cordis Context + SlotRegistry + UI renderer with test-owned session/workspace doubles for feature specs', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-client-ui-agent-preset': { summary: "Agent-preset surfaces: the default for later sessions, this session's seat, and the composition editor", impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-ui-attachment': { summary: 'Dynamic attachment presentation plugin for conversation input and message-image slots', impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-ui-brand-official': { summary: "Official DeepSeek Harness brand occupants for the Web client's sidebar and conversation Hero slots", impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-ui-commands': { summary: "Client command surface: global directory cache, '/' source, three command UI kinds, popupSelect registry", impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-ui-conversation': { summary: 'Conversation domain: skeleton, ordered chat flow, composer with the Host-backed busy-Enter preference, and details host', impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-ui-cordis': { summary: 'Cordis dynamic-plugin definition card: the keyed cordis_define tool row with its run/stop switch', impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-ui-deliverables': { summary: 'Produced-files turn tail and clickable final-response file references for Web', impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-ui-directory-picker-browse': { summary: "In-app directory browsing surface: the workspace directory-flow owner rendering the host's listing and creation primitives", impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-ui-directory-picker-native': { summary: "Native directory-picker surface: the renderless workspace directory-flow occupant driving the host's OS chooser", impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-ui-goal': { summary: 'Session goal surface: GoalBar docked above the composer, read from the goal session projection', impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-ui-input-trigger': { summary: "Input trigger pipeline: '/' and '@' detection, candidate menu, pick routing to registered sources", impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-ui-jobs': { summary: 'Session-header background-job list: live registry state mirrored from session/jobs frames', impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-ui-layout': { summary: 'Shell plugin: three-column AppFrame with drag handles, ctx.layout viewing-state service (navigation + panels)', impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-ui-message-feedback': { summary: 'Per-message feedback controls contributed to the assistant-message action strip, backed by the messageFeedback Host Remote', impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-ui-model-selection': { summary: 'Model selection: the /model popupSelect over session.models / session.selectModel', impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-ui-permission-presets': { summary: 'Permission surfaces: a new-session default in General settings and a current-session /permission popup over the permissions projection', impact: 'Permission/sandbox policy. Disabling weakens approval and sandbox checks.' },
  '@deepseek-ai/dsh-client-ui-plan': { summary: 'Plan-mode composer control: the conversation.input.plan seat over the plan projection and the /plan command channel', impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-ui-primitives': { summary: 'Pure React atoms for the dsh web UI: controls, icons, markdown, and JSON inspectors (zero cordis)', impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-ui-reference': { summary: 'Unified Web @file and @session reference source', impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-ui-renderer': { summary: 'Browser UI renderer: React slot bindings, ctx.uiRenderer, and the assembled application root', impact: 'Disabling blanks the entire web interface; only CLI and ACP remain usable. Re-enable and reload to restore.' },
  '@deepseek-ai/dsh-client-ui-settings': { summary: 'Settings domain base plugin: the settings-namespace scope service and the canonical settings slot-type contract', impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-ui-settings-general': { summary: 'Settings ownerless-copy and product onboarding plugin: the General section, shell trigger/header chrome content, settings dictionaries, and the versioned welcome notice', impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-ui-settings-models': { summary: 'Models settings and shared product-onboarding dialogs over existing settings and credential joins', impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-ui-settings-plugin-inventory': { summary: 'Read-only Cordis Loader inventory tab in Web Plugins settings', impact: 'Hides the Plugin list tab itself. Re-enable via config patch if hidden.' },
  '@deepseek-ai/dsh-client-ui-settings-plugins': { summary: 'Plugins settings section with feature-owned tabs and configurable host-plane plugin cards', impact: 'Removes the Plugin configuration cards. Inventory tab remains.' },
  '@deepseek-ai/dsh-client-ui-sidebar': { summary: 'Sidebar plugin: session multi-level tree, search, grouping, state dots', impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-ui-skill': { summary: 'Web skill references and the dedicated skill tool row', impact: 'Skill discovery/loading. Disabling removes /skill and skill tools.' },
  '@deepseek-ai/dsh-client-ui-slots': { summary: 'Slot registry pure core: SlotMap declaration merging, single register composition API, four-share props types, store-seat types, renderer install seam', impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-ui-subagent': { summary: "Subagent conversation catalog, continuation routing UI, and '@' reference source", impact: 'Sub-agent delegation. Disabling removes delegation tools and child agents.' },
  '@deepseek-ai/dsh-client-ui-theme': { summary: 'Theme plugin: Host bootstrap for the pre-plugin palette; DOM-free ThemeRuntime for light/dark/system state; --dsw-* token styles and Appearance settings row', impact: 'Appearance/localization. Disabling affects UI theme/language only.' },
  '@deepseek-ai/dsh-client-ui-tool': { summary: 'Client Tool call-tree renderer and keyed per-tool presentation slot', impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-ui-trajectory': { summary: 'Trajectory event ledger with an interactive timing overview: pure-consumer plugin registering into the conversation ViewMap (no service)', impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-ui-user-questions': { summary: 'Web ask_user_question feature: host tool mount plus composer-takeover question UI', impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-ui-workflow-run': { summary: 'Durable workflow-run Conversation Node and nested member disclosure for dsh web', impact: 'Workflow engine. Disabling removes the workflow tool and durable runs.' },
  '@deepseek-ai/dsh-client-ui-workspace': { summary: 'Workspace picker plugin: one WorkspacePicker registered into the sidebar and empty-state workspace slots', impact: 'UI surface. Disabling hides its view; host capability remains but becomes headless.' },
  '@deepseek-ai/dsh-client-web': { summary: 'Web boot kernel: static module table, Cordis loader, framework-free boot page, and UI-renderer handoff', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-cmdline': { summary: 'Immutable command-line handoff from a dsh launcher to any app plugin that injects cmdlineArgs', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-code-runtime': { summary: 'Abstract code-execution seam (ctx.codeRuntime) for the DeepSeek Harness', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-code-runtime-python': { summary: 'CPython subprocess implementation of the DeepSeek Harness code-execution seam', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-browser': { summary: 'Abstract browser-automation capability seam (ctx.browser) for the DeepSeek Harness — provider registry, registration-order-independent selection, session/page vocabulary, and the BrowserError taxonomy', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-tool-browser': { summary: 'Model-facing browser tools (browser_navigate/snapshot/click/type/screenshot) over ctx.browser', impact: 'Browser automation. Disabling removes model browser control.' },
  '@deepseek-ai/dsh-code-runtime-worker-thread': { summary: 'Worker-thread implementation of the DeepSeek Harness code-execution seam', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-command-compact': { summary: 'Human-facing slash command for explicit session compaction', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-command-feedback': { summary: 'Log-only session feedback producer and human-facing slash command', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-command-goal': { summary: 'Human-facing slash command for persisted same-session goals', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-commands': { summary: 'Plugin-owned human command registry for DeepSeek Harness UIs', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-compaction': { summary: 'Abstract compaction service seam (ctx.compaction) for the DeepSeek Harness', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-compaction-basic': { summary: 'Token-meter-driven compaction policy and LLM summarization backend for the DeepSeek Harness', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-compaction-tool-result-pruner': { summary: 'Replay-safe model-free head/middle/tail pruning for tool-result surface nodes', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-cordis-client-runner': { summary: 'Browser half of dynamic dual-half plugin packages: event subscription, closure evaluation, guard facade, and loader entries', impact: 'Dynamic Cordis plugin runner (define/run/stop from panel). Disabling removes the Cordis panel.' },
  '@deepseek-ai/dsh-cordis-host-runner': { summary: 'Dynamic package definition registry, host-half sandbox lifecycle, and invoke handler table for model-mounted dual-half packages', impact: 'Dynamic Cordis plugin runner (define/run/stop from panel). Disabling removes the Cordis panel.' },
  '@deepseek-ai/dsh-credentials': { summary: 'Abstract credential seam (ctx.credentials): settings carry references to secrets, providers own the values', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-credentials-local': { summary: 'File-backed credentials provider ($DSH_HOME/.env under the live process environment) for the DeepSeek Harness', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-e2b': { summary: 'Shared E2B sandbox lifecycle for DeepSeek Harness provider adapters', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-experimental-agent-team': { summary: 'Implicit-root Agent Teams roster, durable peer mailbox, and shared task DAG', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-experimental-tool-agent-team': { summary: 'Scoped model-facing Agent Teams tools over ctx.agentTeams', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-file-reference': { summary: 'File-reference discovery contract and shared @file grammar', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-file-reference-local': { summary: 'Local-filesystem ctx.fileReferences provider with bounded fuzzy indexes', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-fs': { summary: 'Abstract filesystem capability seam (ctx.fs) for the DeepSeek Harness — vocabulary types, the FileSystem service (text IO + optional version-guarded atomic mutations), and the fs/* policy event vocabu', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-fs-e2b': { summary: 'E2B filesystem implementation for DeepSeek Harness', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-fs-local': { summary: 'Local-filesystem implementation of the DeepSeek Harness filesystem seam (ctx.fs)', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-fs-observation-policy': { summary: 'File-context policy plugin for the DeepSeek Harness — observed-state, read-before-edit, and version-guarded write/edit added over the ctx.fs provider seam through the fs/* event gate (no service API)', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-fs-sandbox': { summary: 'Sandbox-enforcing implementation of the DeepSeek Harness filesystem seam: fences write/edit by the per-call sandbox mode (read-only denies mutation, workspace-write contains it to the workspace + temp', impact: 'Process/filesystem sandbox. Disabling weakens isolation; tools run unsandboxed.' },
  '@deepseek-ai/dsh-goal': { summary: 'Event-sourced same-session goal state and lifecycle service for the DeepSeek Harness', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-goal-round-driver': { summary: 'Race-fenced same-session goal-round driver', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-headless': { summary: 'The dsh one-shot bundle: a direct core Agent/Session runner over dsh-base with no Host, HTTP, or browser layer', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-home-paths': { summary: 'Shared filesystem path helpers for the DeepSeek Harness', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-hook-protocol': { summary: 'Shared Claude Code / Codex hook wire protocol: matcher engine, stdin/exit-code/stdout codec, multi-hook merge, and hook/* session events', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-hooks-claude-code': { summary: 'Bridge plugin: run a Claude Code hooks.json / settings hook config on the DeepSeek Harness interception seams', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-hooks-codex': { summary: 'Bridge plugin: run a Codex hooks.json hook config on the DeepSeek Harness interception seams', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-host-apiproxy': { summary: 'API gateway: the ApiProxy contract (api/), the fetch carrier pair (fetch/), and the host-side gateway plugin providing ctx.apiProxy', impact: 'Host capability. Disabling removes that server-side feature for all sessions.' },
  '@deepseek-ai/dsh-host-directory-picker': { summary: 'Abstract workspace-directory picking seam (ctx.directoryPicker) for the DeepSeek Harness web GUI host', impact: 'Host capability. Disabling removes that server-side feature for all sessions.' },
  '@deepseek-ai/dsh-host-directory-picker-auto': { summary: 'Adaptive chooser of the directory-picker seam: resolves the host situation at boot and mounts the native or browse backend for the DeepSeek Harness web GUI host', impact: 'Host capability. Disabling removes that server-side feature for all sessions.' },
  '@deepseek-ai/dsh-host-directory-picker-browse': { summary: 'In-app browsing backend of the directory-picker seam (listing/creation primitives over the host filesystem)', impact: 'Host capability. Disabling removes that server-side feature for all sessions.' },
  '@deepseek-ai/dsh-host-directory-picker-native': { summary: 'Native-OS-chooser backend of the directory-picker seam for the DeepSeek Harness web GUI host', impact: 'Host capability. Disabling removes that server-side feature for all sessions.' },
  '@deepseek-ai/dsh-host-frontend-static': { summary: 'SPA dist server for the Web shell: owns the webserver fallback seat, serving the built frontend with index-tap injection, traversal rejection, and SPA index fallback', impact: 'Host capability. Disabling removes that server-side feature for all sessions.' },
  '@deepseek-ai/dsh-host-plugin-inventory': { summary: 'Read-only Remote projection of current Cordis Loader plugin state', impact: 'Host capability. Disabling removes that server-side feature for all sessions.' },
  '@deepseek-ai/dsh-host-webserver': { summary: 'Web route-registration plugin: HTTP and upgrade routes, index transform taps, and static dist fallback; knows no harness concepts', impact: 'Host capability. Disabling removes that server-side feature for all sessions.' },
  '@deepseek-ai/dsh-invariants': { summary: 'Registry service for package-owned DeepSeek Harness runtime invariants', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-jobs': { summary: 'Background job registry (ctx.jobs) for the DeepSeek Harness — shared ids, owner isolation, polling, cancellation, and completion listeners for long-running tool work', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-jobs-local': { summary: 'Process-local implementation of the DeepSeek Harness background job registry seam', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-launch-environment': { summary: 'Immutable DeepSeek Harness launch environment that records which layer supplied each value', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-llm': { summary: 'Provider-neutral LLM service interface for the DeepSeek Harness', impact: 'LLM provider. Disabling removes model access; agent stays idle and tools still listed but no completions.' },
  '@deepseek-ai/dsh-llm-ai-sdk': { summary: 'AI SDK sidecar adapter: every provider route (DeepSeek, OpenRouter, Ollama, Anthropic, Gemini, gateways) resolves through one ai-sidecar child process', impact: 'LLM provider. Disabling removes model access; agent stays idle and tools still listed but no completions.' },
  '@deepseek-ai/dsh-llm-mock-server': { summary: 'Scriptable OpenAI-compatible HTTP/SSE fault server for LLM recovery tests', impact: 'LLM provider. Disabling removes model access; agent stays idle and tools still listed but no completions.' },

  '@deepseek-ai/dsh-llm-replay': { summary: 'Replay LLM plugin: short-circuits llm/stream with model chunks reconstructed from a recorded session JSONL (keyless snapshot tests)', impact: 'LLM provider. Disabling removes model access; agent stays idle and tools still listed but no completions.' },
  '@deepseek-ai/dsh-llm-retry': { summary: 'Provider-routed LLM request retry policy for the DeepSeek Harness', impact: 'LLM provider. Disabling removes model access; agent stays idle and tools still listed but no completions.' },
  '@deepseek-ai/dsh-loader-smoke': { summary: 'Shared subprocess and direct-agent harness for keyless real-Loader example smoke tests', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-lsp': { summary: 'Abstract LSP capability seam (ctx.lsp) for the DeepSeek Harness — language-server provider registry keyed by branded id and extension mapping, order-independent per-query selection, normalized definit', impact: 'Code navigation (go-to-definition etc.). Disabling removes LSP tools.' },
  '@deepseek-ai/dsh-lsp-stdio': { summary: 'Generic stdio language-server provider for the DeepSeek Harness LSP capability seam (ctx.lsp) — spawns configured servers, translates JSON-RPC, and serves transient-open goToDefinition/findReferences/', impact: 'Code navigation (go-to-definition etc.). Disabling removes LSP tools.' },
  '@deepseek-ai/dsh-mcp-client': { summary: 'MCP client bridge: connects to MCP servers and registers their tools on ctx.tools', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-message-feedback': { summary: 'Lifecycle-bound per-message rating and note sidecar for the DeepSeek Harness', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-native-command': { summary: 'Zero-dependency no-shell execFile runner for host-native OS integrations: utf8 stdio capture, abort propagation, Windows hide', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-output-retention': { summary: 'Zero-dependency bounded-retention primitive: ItemRetainer/TextRetainer + neutral notice helpers (what did we keep, what did we omit)', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-permission-presets': { summary: 'User-facing permission presets (ctx.permissionPresets) for the DeepSeek Harness: one product-level Permissions select bundling the sandbox-mode and approval-policy knobs, written through to their own ', impact: 'Permission/sandbox policy. Disabling weakens approval and sandbox checks.' },
  '@deepseek-ai/dsh-persona': { summary: 'Composition-authored deployment persona section for the DeepSeek Harness', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-plan-mode': { summary: 'Logged per-agent plan mode with deployment guidance, a direct slash command, and a user-reviewed exit', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-pwsh-local': { summary: 'Local PowerShell implementation of the DeepSeek Harness bash executor seam', impact: 'Shell command execution. Disabling removes the agent’s ability to run commands.' },
  '@deepseek-ai/dsh-pwsh-sandbox': { summary: 'Sandbox-consuming implementation of the DeepSeek Harness PowerShell executor seam (confines every command via ctx.sandbox, reports denial/enforcement result facts)', impact: 'Shell command execution. Disabling removes the agent’s ability to run commands.' },
  '@deepseek-ai/dsh-repeat-tool-reminder': { summary: 'Repeat-tool-call guard plugin: advisory reminders when an agent loops on identical tool calls', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-sandbox': { summary: 'Abstract process-sandbox seam (ctx.sandbox) for the DeepSeek Harness: same-world confinement vocabulary and the SandboxProvider contract', impact: 'Process/filesystem sandbox. Disabling weakens isolation; tools run unsandboxed.' },
  '@deepseek-ai/dsh-sandbox-local': { summary: 'Local process-sandbox backends for the DeepSeek Harness sandbox seam: bwrap, the npm-distributed landlock-run launcher, macOS Seatbelt, or the Windows ACL restricted-token runner — functionally probed', impact: 'Process/filesystem sandbox. Disabling weakens isolation; tools run unsandboxed.' },
  '@deepseek-ai/dsh-sandbox-policy': { summary: "Per-call sandbox policy resolver and current model context: deployment fallbacks plus each session's mode and workspace root, shared by every enforcing capability family", impact: 'Process/filesystem sandbox. Disabling weakens isolation; tools run unsandboxed.' },
  '@deepseek-ai/dsh-sandbox-windows-acl': { summary: 'Windows ACL write-restriction sandbox backend (restricted-token spawn with capability-SID write allowlist) for the DeepSeek Harness sandbox seam', impact: 'Process/filesystem sandbox. Disabling weakens isolation; tools run unsandboxed.' },
  '@deepseek-ai/dsh-schedule': { summary: 'Agent-scoped durable after, at, and fixed-rate reminders over the session event log', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-scope': { summary: 'Scoped-context registration primitive (scope tags, scope-filtered event dispatch) for the DeepSeek Harness', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-sdk-client': { summary: 'TypeScript client SDK for driving a DeepSeek Harness runtime subprocess over stdio JSON-RPC: the DeepSeekHarness high-level turns API and the lower-level HarnessClient', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-sdk-jsonrpc-demo': { summary: 'Bin that boots an external Cordis config for the stdio JSON-RPC SDK runtime', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-sdk-jsonrpc-server': { summary: 'Stdio JSON-RPC server plugin for out-of-process DeepSeek Harness SDK clients', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-sdk-protocol': { summary: 'Shared wire protocol for the DeepSeek Harness SDK runtime: the newline-delimited JSON-RPC stdio transport and the named request, result, and notification types spoken between the runtime server and SD', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-session': { summary: 'Event-sourced session store for the DeepSeek Harness', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-session-checkpoint-policy': { summary: 'Semantic session durability checkpoints before model requests and tool side effects', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-session-log-export': { summary: 'Web Session-log export command and shared download dialog', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-session-persistence': { summary: 'Abstract durable session persistence seam (ctx.sessionPersistence) for the DeepSeek Harness', impact: 'Durable session storage. Disabling loses history after restart.' },
  '@deepseek-ai/dsh-session-persistence-jsonl': { summary: 'JSONL durable session persistence backend for the DeepSeek Harness', impact: 'Durable session storage. Disabling loses history after restart.' },
  '@deepseek-ai/dsh-session-persistence-sqlite': { summary: 'SQLite durable session persistence with physical chunk-row packing', impact: 'Durable session storage. Disabling loses history after restart.' },
  '@deepseek-ai/dsh-session-projection': { summary: 'Session-projection seam: the merge-extensible projection type table, the provider contract, and the ctx.sessionProjections registry serving whole current values of log-derived per-session state', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-session-projection-cache': { summary: 'Persisted projection cache (ctx.sessionProjectionCache): durable per-session projection checkpoints over the domain data form, throttled write-behind, and the cold-read ladder (cache row + persistence', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-session-query': { summary: 'Combined session query service contract with concrete reads, traces, and filters', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-session-query-sqlite': { summary: 'Concrete ctx.sessionQuery backend with SQLite FTS5 search', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-session-reference': { summary: 'Cross-session snapshot references and durable untrusted model context (ctx.sessionReferenceResolver)', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-session-stats': { summary: 'Whole-log conversation counts and wall times projection (sessionStats) for the DeepSeek Harness', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-session-telemetry': { summary: 'SessionTelemetryBackend seam for the DeepSeek Harness: session-event capture, projection, redaction, and handoff to a reporting backend', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-session-telemetry-otel': { summary: "OpenTelemetry backend for the DeepSeek Harness telemetry seam: hands captured session records to the OTel JS SDK's log pipeline", impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-session-title': { summary: 'Log-backed session title service and provider registry for the DeepSeek Harness', impact: 'Auto-titling sessions. Disabling keeps titles as “Untitled”.' },
  '@deepseek-ai/dsh-session-title-all-prompts-llm': { summary: 'All-user-messages LLM provider plugin for DeepSeek Harness session titles', impact: 'LLM provider. Disabling removes model access; agent stays idle and tools still listed but no completions.' },
  '@deepseek-ai/dsh-session-title-first-prompt-llm': { summary: 'First-message LLM provider plugin for DeepSeek Harness session titles', impact: 'LLM provider. Disabling removes model access; agent stays idle and tools still listed but no completions.' },
  '@deepseek-ai/dsh-session-title-llm': { summary: 'Shared LLM generation policy for DeepSeek Harness session-title providers', impact: 'LLM provider. Disabling removes model access; agent stays idle and tools still listed but no completions.' },
  '@deepseek-ai/dsh-settings': { summary: 'Abstract user-settings seam (ctx.settings) for the DeepSeek Harness', impact: 'Host capability. Disabling removes that server-side feature for all sessions.' },
  '@deepseek-ai/dsh-settings-file': { summary: 'File-backed settings provider (settings.yaml) for the DeepSeek Harness', impact: 'Host capability. Disabling removes that server-side feature for all sessions.' },
  '@deepseek-ai/dsh-shell': { summary: 'Abstract bash executor seam (ctx.shell) for the DeepSeek Harness', impact: 'Shell command execution. Disabling removes the agent’s ability to run commands.' },
  '@deepseek-ai/dsh-shell-env': { summary: 'Tool-independent managed DSH_* shell environment registry', impact: 'Shell command execution. Disabling removes the agent’s ability to run commands.' },
  '@deepseek-ai/dsh-skill': { summary: 'Agent skill provider registry for the DeepSeek Harness', impact: 'Skill discovery/loading. Disabling removes /skill and skill tools.' },
  '@deepseek-ai/dsh-skill-badge': { summary: 'Bundled dsh badge skill provider for DeepSeek Harness', impact: 'Skill discovery/loading. Disabling removes /skill and skill tools.' },
  '@deepseek-ai/dsh-skill-filesystem': { summary: 'Local filesystem skill provider for the DeepSeek Harness', impact: 'Skill discovery/loading. Disabling removes /skill and skill tools.' },
  '@deepseek-ai/dsh-spill': { summary: 'Abstract spill storage seam (ctx.spillStore) for the DeepSeek Harness — save oversized tool text and return a retrieval locator', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-spill-local': { summary: 'Local-filesystem implementation of the DeepSeek Harness spill storage seam (private session-scoped files)', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-spill-policy': { summary: 'Tool-result spill policy for the DeepSeek Harness — replaces oversized plain-text tool results with a retained preview plus a spill-file path (no service API)', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-storage': { summary: 'Storage hub (ctx.storage): named backend registry plus mounted data-form facilities for the DeepSeek Harness', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-storage-domain': { summary: 'Domain data form (ctx.storage.domain): schema-validated, event-emitting KV domains over storage backends for the DeepSeek Harness', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-storage-json': { summary: 'JSON file KV storage backend for the DeepSeek Harness storage hub', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-storage-sqlite': { summary: 'SQLite storage backend (kv facet) for the DeepSeek Harness storage hub', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-subagent': { summary: 'Abstract subagent seam (ctx.subagents): named-provider registry for delegating to child agents', impact: 'Sub-agent delegation. Disabling removes delegation tools and child agents.' },
  '@deepseek-ai/dsh-subagent-acp': { summary: 'Out-of-process ACP subagent backend: drives a child agent in a spawned subprocess over the Agent Client Protocol', impact: 'Sub-agent delegation. Disabling removes delegation tools and child agents.' },
  '@deepseek-ai/dsh-subagent-claude-code': { summary: 'One-shot Claude Code subagent provider over the official Agent SDK', impact: 'Sub-agent delegation. Disabling removes delegation tools and child agents.' },
  '@deepseek-ai/dsh-subagent-codex': { summary: 'One-shot Codex subagent provider over the official app-server protocol', impact: 'Sub-agent delegation. Disabling removes delegation tools and child agents.' },
  '@deepseek-ai/dsh-subagent-dsh-sdk': { summary: 'Out-of-process SDK subagent backend: drives a child DeepSeek Harness runtime subprocess over stdio JSON-RPC through the TypeScript SDK client', impact: 'Sub-agent delegation. Disabling removes delegation tools and child agents.' },
  '@deepseek-ai/dsh-subagent-fork-in-process': { summary: "In-process fork subagent backend: runs a child agent seeded with a prefix of the parent's log", impact: 'Sub-agent delegation. Disabling removes delegation tools and child agents.' },
  '@deepseek-ai/dsh-subagent-in-process-driver': { summary: 'Shared in-process subagent run driver: drives a child agent on ctx.agents (used by the spawn and fork backends)', impact: 'Sub-agent delegation. Disabling removes delegation tools and child agents.' },
  '@deepseek-ai/dsh-subagent-spawn-in-process': { summary: 'In-process spawn subagent backend: runs a fresh child agent on ctx.agents', impact: 'Sub-agent delegation. Disabling removes delegation tools and child agents.' },
  '@deepseek-ai/dsh-subprocess': { summary: 'Subprocess seam (ctx.subprocess) for the DeepSeek Harness — managed process groups, bounded spill-backed output, and escalated kills behind one abstract service', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-subprocess-e2b': { summary: 'E2B subprocess implementation for DeepSeek Harness', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-subprocess-local': { summary: 'Local-subprocess implementation of the DeepSeek Harness subprocess seam', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-system-prompt': { summary: 'System prompt assembly registry for the DeepSeek Harness', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-terminal': { summary: 'Persistent PTY session seam for the DeepSeek Harness — owner-scoped ids, backend registry, interactive sends, reads, signals, and awaited cleanup', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-terminal-bash': { summary: 'Persistent shell PTY backend over the DeepSeek Harness subprocess terminal primitive', impact: 'Shell command execution. Disabling removes the agent’s ability to run commands.' },
  '@deepseek-ai/dsh-time-context': { summary: 'Opt-in durable per-step context with the current time and elapsed time', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-timeout': { summary: 'Zero-dependency timeout/deadline primitive: clampTimeout, deadline, timeoutOf, TimeoutReason (timing + classification only, no termination)', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-tmux-context': { summary: "Opt-in durable per-step context with this agent's tmux pane and window location", impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-token-meter': { summary: 'Replay-aware token measurement service (ctx.tokenMeter) for the DeepSeek Harness', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-tool-ask-user': { summary: 'Model-facing ask_user_question tool over the ctx.userQuestions seam', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-tool-bash': { summary: 'Model-facing bash tool with optional generic background-job and sandbox-escalation support', impact: 'Shell command execution. Disabling removes the agent’s ability to run commands.' },
  '@deepseek-ai/dsh-tool-bash-persistent': { summary: 'Model-facing owner-scoped persistent Bash tool backed by the Harness PTY service', impact: 'Shell command execution. Disabling removes the agent’s ability to run commands.' },
  '@deepseek-ai/dsh-tool-call-timeout-policy': { summary: 'Tool-call timeout policy: a tools/execute wrapper that arms a per-tool deadline on exec.signal and returns TOOL_TIMEOUT when it wins', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-tool-cordis': { summary: 'Self-referential cordis toolset: inspect the live runtime, mount and dispose model-written plugins', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-tool-fs': { summary: 'Model-facing filesystem tools (read, write, edit) over the DeepSeek Harness filesystem seam (ctx.fs)', impact: 'File read/write/search tools. Disabling removes workspace file access.' },
  '@deepseek-ai/dsh-tool-fs-search': { summary: 'Model-facing filesystem discovery tools (glob, grep) backed by the packaged ripgrep binary (@vscode/ripgrep)', impact: 'File read/write/search tools. Disabling removes workspace file access.' },
  '@deepseek-ai/dsh-tool-goal': { summary: 'Model-facing same-session goal tools with execution-time authority checks', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-tool-jobs': { summary: 'Model-facing background job control tools (job_output, job_list, job_kill) over the ctx.jobs registry', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-tool-lsp': { summary: 'Model-facing lsp tool over the DeepSeek Harness LSP capability seam (ctx.lsp) — one read-only tool with goToDefinition/findReferences/goToImplementation/hover operations, one-based UTF-16 cursor coord', impact: 'Code navigation (go-to-definition etc.). Disabling removes LSP tools.' },
  '@deepseek-ai/dsh-tool-pwsh': { summary: 'Model-facing pwsh tool over the bash executor seam', impact: 'Shell command execution. Disabling removes the agent’s ability to run commands.' },
  '@deepseek-ai/dsh-tool-pwsh-persistent': { summary: 'Model-facing owner-scoped persistent PowerShell tool backed by the Harness PTY service', impact: 'Shell command execution. Disabling removes the agent’s ability to run commands.' },
  '@deepseek-ai/dsh-tool-ralph': { summary: 'Model-facing fresh-agent Ralph loop over the workflow and subagent seams', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-tool-session-query': { summary: 'Workspace-authorized model-facing session history search, trace, and event read tools', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-tool-skill': { summary: 'Model-facing skill loading tool for the DeepSeek Harness', impact: 'Skill discovery/loading. Disabling removes /skill and skill tools.' },
  '@deepseek-ai/dsh-tool-str-replace-editor': { summary: 'Model-facing view, create, literal replace, and line insert tool over the Harness filesystem service', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-tool-subagent': { summary: 'Model-facing subagent delegation tool over the ctx.subagents seam', impact: 'Sub-agent delegation. Disabling removes delegation tools and child agents.' },
  '@deepseek-ai/dsh-tool-subagent-control': { summary: 'Globally named send_message, interrupt_agent, and list_agents tools over ctx.subagents continuations', impact: 'Sub-agent delegation. Disabling removes delegation tools and child agents.' },
  '@deepseek-ai/dsh-tool-subagent-report': { summary: 'Child-scoped report tool over ctx.subagents continuations', impact: 'Sub-agent delegation. Disabling removes delegation tools and child agents.' },
  '@deepseek-ai/dsh-tool-terminal': { summary: 'Six model-facing persistent PTY tools with owner isolation and generic background-job integration', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-tool-todo': { summary: 'Model-facing todo_write tool over the DeepSeek Harness event-sourced session log', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-tool-web': { summary: 'Model-facing web tools (web_search, web_fetch) over the DeepSeek Harness web capability seam (ctx.web)', impact: 'Web search/fetch. Disabling removes internet browsing.' },
  '@deepseek-ai/dsh-tool-workflow': { summary: 'Model-facing workflow tool: run a JavaScript orchestration script over ctx.workflowEngine', impact: 'Workflow engine. Disabling removes the workflow tool and durable runs.' },
  '@deepseek-ai/dsh-tools': { summary: 'Tool registry and execution pipeline for the DeepSeek Harness', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-typert-generator': { summary: 'TypeScript project analyzer and model-driven Typert artifact generator', impact: 'RPC gateway. Disabling breaks all Remote calls (settings, inventory, etc.).' },
  '@deepseek-ai/dsh-typert-loader': { summary: 'Loader integration for generated Typert package contributions', impact: 'RPC gateway. Disabling breaks all Remote calls (settings, inventory, etc.).' },
  '@deepseek-ai/dsh-typert-protocol': { summary: 'Compiler-independent Remote metadata and Typert provider protocols', impact: 'RPC gateway. Disabling breaks all Remote calls (settings, inventory, etc.).' },
  '@deepseek-ai/dsh-typert-registry': { summary: 'Runtime registry for generated package reflection and Zod schemas', impact: 'RPC gateway. Disabling breaks all Remote calls (settings, inventory, etc.).' },
  '@deepseek-ai/dsh-user-approval': { summary: 'User-approval seam (ctx.approval) for the DeepSeek Harness: one-shot permission decisions dispatched to composed answerers over the approval/request waterfall, fail-closed by default', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-user-questions': { summary: 'Abstract user-questions seam (ctx.userQuestions) for asking the human during agent runs', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-web': { summary: 'Abstract web access capability seam (ctx.web) for the DeepSeek Harness — search/fetch provider registry, registration-order-independent selection, request/result vocabulary, and the WebError taxonomy', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-web-app': { summary: 'The dsh browser-surface bundle: the web patch layer over dsh-base plus the runtime glue plugin (frontend dist serving, web-surface prompt, bash runtime variables, URL line)', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/dsh-web-fetch-http': { summary: 'Anonymous public HTTP(S) fetch provider for the DeepSeek Harness web capability seam (ctx.web)', impact: 'Web search/fetch. Disabling removes internet browsing.' },
  '@deepseek-ai/dsh-web-search-deepseek': { summary: 'DeepSeek-backed search provider (native web_search via the Anthropic-compatible API) for the DeepSeek Harness web capability seam (ctx.web)', impact: 'Web search/fetch. Disabling removes internet browsing.' },
  '@deepseek-ai/dsh-web-search-exa': { summary: 'Exa-backed search provider for the DeepSeek Harness web capability seam (ctx.web)', impact: 'Web search/fetch. Disabling removes internet browsing.' },
  '@deepseek-ai/dsh-web-search-perplexity': { summary: 'Perplexity-backed search provider for the DeepSeek Harness web capability seam (ctx.web)', impact: 'Web search/fetch. Disabling removes internet browsing.' },
  '@deepseek-ai/dsh-web-search-searxng': { summary: 'SearXNG-backed search provider for the DeepSeek Harness web capability seam (ctx.web): queries a self-hosted SearXNG instance JSON API', impact: 'Web search/fetch. Disabling removes the searxng backend.' },
  '@deepseek-ai/dsh-workflow': { summary: 'Workflow capability seam: ctx.workflowEngine service, run vocabulary, and workflow/* events', impact: 'Workflow engine. Disabling removes the workflow tool and durable runs.' },
  '@deepseek-ai/dsh-workflow-worker-thread': { summary: 'worker-thread workflow engine: executes model-written orchestration scripts off the host event loop, bridging agent() calls back to ctx.subagents', impact: 'Workflow engine. Disabling removes the workflow tool and durable runs.' },
  '@deepseek-ai/dsh-workspace': { summary: 'Workspace entity registry (ctx.workspaceRegistry): durable workspace records with validated session attachment over the domain data form for the DeepSeek Harness', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
  '@deepseek-ai/schemastery': { summary: 'Type driven schema validator', impact: 'Disabling removes this feature; re-enable to restore it. Requires host restart if the plugin already failed to apply.' },
}
/**
 * Resolve one plugin module's inventory summary and impact fallback.
 * @param moduleName - the plugin package or builtin name as mounted.
 * @returns the catalog entry, or a derived fallback for unknown names.
 */
export function getPluginMeta(moduleName: string): PluginMeta {
  const exact = CATALOG[moduleName]
  if (exact) return exact
  // Try without scope: @deepseek-ai/cordis-plugin-hmr -> cordis-plugin-hmr -> hmr
  const unscoped = moduleName.startsWith('@') ? moduleName.slice(moduleName.indexOf('/')+1) : moduleName
  if (CATALOG[unscoped]) return CATALOG[unscoped] as PluginMeta
  const short = unscoped.replace(/^cordis:/,'').replace(/^cordis-plugin-/, '').replace(/^dsh-(?:host-|client-)?/, '').replace(/^cordis-/, '')
  // also try short name lookup
  for (const key of Object.keys(CATALOG)) {
    if (key.endsWith('/' + short) || key === short) return CATALOG[key] as PluginMeta
  }
  const builtin = 'cordis:' + short
  const found = CATALOG[builtin]
  /* v8 ignore next 2 -- no cordis builtin is cataloged separately */
  if (found) return found
  if (moduleName.startsWith('cordis:')) {
    return { summary: 'Cordis builtin: ' + short, impact: 'Builtin — disabling is not applicable; it is provided by the runtime.' }
  }
  const human = short.replace(/^dsh-/, '').replace(/-/g, ' ')
  return { summary: 'Plugin ' + human, impact: 'Disabling removes this feature; re-enable to restore it.' }
}
/**
 * True for plugins the shell cannot render without.
 * @param moduleName - the plugin package name as mounted.
 * @returns whether disabling it would break the assembled browser shell.
 */
/* v8 ignore next 3 -- essential check is simple predicate, covered via component rendering */
export function isEssentialPlugin(moduleName: string): boolean {
  return moduleName.includes('ui-renderer') || moduleName.includes('client-modules') || moduleName.includes('client-runtime') || moduleName.includes('connection') || moduleName.includes('host-plugin-inventory') || moduleName.includes('cordis-plugin-loader')
}
