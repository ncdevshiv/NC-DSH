[package]
license.workspace = true
name = "moli-crypto"
version = "0.1.0"
edition = "2024"
description = "Backend-neutral digest and secure-random primitives for Moli"

[dependencies]
aws-lc-rs = { version = "1.16.2", default-features = false, features = ["aws-lc-sys"] }

[lints]
workspace = true
